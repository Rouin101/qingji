# 青迹项目交接说明

> 工作区：`C:\Users\葛浩东\Desktop\EL`  
> 初始整理：2026-08-01  
> 最新更新：2026-08-22

## 1. 已确认的决定

- 项目：**青迹——面向高校社会实践的可信证据链智能体**。
- 开发方式：葛浩东单人独立开发。
- 赛事方已确认：**可以不使用扣子工具**，自研技术路线不再构成参赛资格风险。
- 当前仍保持文字材料闭环；v1.1 已加入默认关闭、用户主动触发的联网大模型建议层；ASR、OCR、多智能体与知识图谱属于后续增强。
- 内置项目已经准备好完整材料、证据和结论流程；新增材料须如实记录来源和授权。

## 2. 产品定位与边界

青迹将经授权的社会实践材料整理为可追溯证据卡，判断待写入报告的表述能否被当前材料支持，并把证据缺口转换为补证任务。

它判断的是“当前项目材料的支持程度”，不是现实世界真伪、伦理审查或法律意见。四级结果为：

- 已有支持 `supported`
- 部分支持 `partially_supported`
- 暂无支持 `unsupported`
- 存在冲突 `contradicted`

## 3. 当前完成状态

文字版 v1.0 闭环已完成：

```text
导入经授权文字材料
→ 本地脱敏
→ 生成并人工审核证据卡
→ 输入待核验结论
→ 本地检索与四级判断
→ 生成稳妥改写和补证任务
→ 加入新材料后重新核验
→ 导出可追溯 Markdown
→ 完整备份并恢复为新项目
```

主要模块均已落盘：SQLite/FTS5、脱敏、证据生成、本地检索、规则核验、项目数据、工作流编排、Streamlit 多页应用和 Markdown 导出。

项目工作区已支持创建、切换、重命名、归档、恢复和安全删除。永久删除只允许用于已归档的用户项目，要求输入完整项目名称，并同步清理该项目对应的本地原始与脱敏文字文件；内置项目受到保护。

结论历史已支持按四级状态和关键词筛选、切换查看任意记录，并可从“成果与缺口”返回指定结论继续核验；项目概览显示四级状态分布。

每次新核验或重新核验都会持久化一份检索诊断，记录候选证据排名、相关分、关键词与字符片段命中、阈值判断、最终引用关系，以及因未授权或未审核而被排除的材料。固定检查集已扩展为 6 个分类样例，覆盖直接匹配、同义改写、无关主题零命中和正反观点同时召回；当前 Retrieval regression@3 为 6/6，仅用于防止检索规则退化，不代表所有项目的准确率。

用户项目可在“成果与缺口 → 检索评测”下载项目专属 CSV 模板并上传自定义评测集。系统校验目标证据是否属于当前项目、是否已确认授权和完成人工批准，然后在本地运行并保存历史结果；单次最多 100 个用例、文件上限 1 MiB。历史页记录用例集指纹、证据集指纹、检索版本、Top-K、阈值、分类结果与可比性，仅对语料和配置完全一致的运行计算变化；每次运行均可导出 CSV 和 Markdown。

材料页同时支持单条录入和批量导入多个 `.txt`/`.md`/`.docx`/`.pdf` 文件。未加密 Word 和可提取文本的 PDF 会在本地提取正文；扫描版 PDF 若没有文字层需要先 OCR 或转为可复制文字。批量导入先统一检查格式、正文和来源元数据，确认无误后逐份写入本地数据库、保存原文与脱敏文本，并按授权状态生成证据卡；单个文件失败不会阻止其余文件继续处理。

证据卡被批准、撤回或编辑后，系统会自动重新核验当前项目的全部既有结论，并同步系统生成的补证任务，避免旧结论继续引用已撤回证据。存储层同时禁止材料、片段、证据、结论、任务、完成材料和运行记录跨项目重挂。

每次人工审核的实际变更都必须填写说明，并追加写入证据审核事件：记录修改前后状态、变更字段、时间和触发重核的结论。页面按证据展示审核时间线，可信 Markdown 导出包含项目级审核日志；公共存储 API 不提供日志修改或删除能力。旧数据不会被伪造成人工审核记录。

项目概览已支持生成 v1 格式 ZIP 完整备份。备份包含项目数据库关系、审核与评测历史，以及青迹托管的原文和脱敏文件；清单记录格式版本、数量、大小和 SHA-256 哈希，并移除本机绝对路径。恢复前校验安全路径、大小、哈希和跨表引用，始终以新名称创建项目并重新映射内部编号；失败时数据库事务回滚并清理新写入文件。

## 4. 2026-08-22 质量检查结果

- Python 3.10.20。
- Streamlit 1.60.0 已安装至项目 `.venv`。
- `python -m unittest discover -s tests -v`：78 项全部通过，包含项目工作区、完整备份与事务式恢复、旧数据库迁移、安全删除、证据审核日志、结论历史、证据撤回自动重核、任务同步、检索诊断、评测历史与导出、Word/PDF 文本提取、结论与证据卡模型辅助边界、DeepSeek 联调探针、单条与批量材料导入、表单输入保留、项目进度提示、跨项目隔离及四页 AppTest 页面检查。
- `python scripts\eval_retrieval.py`：固定检查集 Retrieval regression@3 = 6/6（100%）。
- `python -m compileall -q app.py qingji pages scripts tests`：通过。
- `python scripts\e2e_apptest.py`：完整纵向闭环及备份恢复通过。
- 独立浏览器验收已走通“生成 → 下载 → 上传校验 → 改名恢复”，页面和控制台无错误。
- 端到端脚本输出已改为 Windows GBK 终端可安全显示的 `[OK]`。

## 5. 运行与验证

启动应用：

```powershell
.\scripts\run.ps1
```

运行全部自动化检查：

```powershell
.\scripts\test.ps1
```

两个脚本优先使用 `.venv\Scripts\python.exe`，如果没有项目虚拟环境才回退到系统 `python`。

## 6. Git 与数据安全

- Git 仓库已初始化，产品阶段性改动均保留在本地提交历史中。
- `.venv/`、`.env`、SQLite 数据库、原始材料、脱敏材料和 Streamlit 密钥均已纳入忽略规则。
- 提交前应再执行一次范围检查，确认没有个人信息或真实调研数据。

## 7. 当前限制

- 结论核验仍由确定性规则负责；大模型目前只是默认关闭的可选建议层，不应把模型建议当作事实判断或规则结论。
- v1.1 当前已完成两类用户主动触发的模型辅助：结论页提供保守改写和补证建议，材料页为已确认授权的证据卡草拟标题、摘要和类型；两者均边界再次脱敏，结构化输出受校验，建议和失败记录写入本地运行历史，刷新页面后可恢复与当前结论/证据版本匹配的建议，且不会替代人工审核或四级规则结论。另有不携带项目材料的 DeepSeek 联调探针。
- 检索基于关键词和中文字符 n-gram，语义改写过大时可能召回不足。
- 只支持文字材料，未接入 ASR、OCR、多智能体或知识图谱。
- 已支持创建和切换本地多项目，但仍是单用户应用，没有账号和多人权限系统。
- 页面备份包最大 50 MiB、解压内容最大 100 MiB；当前不是增量备份或云同步服务。

## 8. 下一阶段建议

1. 用一组已获授权且人工复核的真实项目材料开展小规模试用，积累项目专属评测集和错误分析，但不要把开发集成绩当作外部验证。
2. 在 v1.1 以可关闭、可回退的方式接入大模型辅助证据摘要、保守改写和补证建议；规则校验、引用编号检查与人工审核继续独立运行。
