# 青迹项目交接说明

> 工作区：`C:\Users\葛浩东\Desktop\EL`  
> 初始整理：2026-08-01  
> 最新更新：2026-08-19

## 1. 已确认的决定

- 项目：**青迹——面向高校社会实践的可信证据链智能体**。
- 开发方式：葛浩东单人独立开发。
- 赛事方已确认：**可以不使用扣子工具**，自研技术路线不再构成参赛资格风险。
- v0.1 先保持文字材料闭环；ASR、OCR、联网大模型均属于后续增强。
- 仓库内演示材料均为虚构测试数据，不得当作真实调研或采访结果。

## 2. 产品定位与边界

青迹将经授权的社会实践材料整理为可追溯证据卡，判断待写入报告的表述能否被当前材料支持，并把证据缺口转换为补证任务。

它判断的是“当前项目材料的支持程度”，不是现实世界真伪、伦理审查或法律意见。四级结果为：

- 已有支持 `supported`
- 部分支持 `partially_supported`
- 暂无支持 `unsupported`
- 存在冲突 `contradicted`

## 3. 当前完成状态

文字版 v0.6 闭环已完成：

```text
导入经授权文字材料
→ 本地脱敏
→ 生成并人工审核证据卡
→ 输入待核验结论
→ 本地检索与四级判断
→ 生成稳妥改写和补证任务
→ 加入新材料后重新核验
→ 导出可追溯 Markdown
```

主要模块均已落盘：SQLite/FTS5、脱敏、证据生成、本地检索、规则核验、虚构演示数据、工作流编排、Streamlit 多页应用和 Markdown 导出。

项目工作区已支持创建、切换、重命名、归档、恢复和安全删除。永久删除只允许用于已归档的用户项目，要求输入完整项目名称，并同步清理该项目对应的本地原始与脱敏文字文件；内置虚构测试项目受到保护。

结论历史已支持按四级状态和关键词筛选、切换查看任意记录，并可从“成果与缺口”返回指定结论继续核验；项目概览显示四级状态分布。

每次新核验或重新核验都会持久化一份检索诊断，记录候选证据排名、相关分、关键词与字符片段命中、阈值判断、最终引用关系，以及因未授权或未审核而被排除的材料。固定虚构回归集已扩展为 6 个分类样例，覆盖直接匹配、同义改写、无关主题零命中和正反观点同时召回；当前 Retrieval regression@3 为 6/6，仅用于防止检索规则退化，不代表真实项目准确率。

## 4. 2026-08-19 验证结果

- Python 3.10.20。
- Streamlit 1.60.0 已安装至项目 `.venv`。
- `python -m unittest discover -s tests -v`：37 项全部通过，包含项目工作区、旧数据库迁移、安全删除、结论历史、检索诊断与分类召回评测、跨项目状态隔离及四页 AppTest 冒烟测试。
- `python scripts\eval_retrieval.py`：固定虚构回归集 Retrieval regression@3 = 6/6（100%）。
- `python -m compileall -q app.py qingji pages scripts tests`：通过。
- `python scripts\e2e_apptest.py`：完整纵向闭环通过。
- 端到端脚本输出已改为 Windows GBK 终端可安全显示的 `[OK]`。

## 5. 运行与验证

启动应用：

```powershell
.\scripts\run.ps1
```

运行全部自动化检查：

```powershell
.\scripts\test.ps1
```

两个脚本优先使用 `.venv\Scripts\python.exe`，如果没有项目虚拟环境才回退到系统 `python`。

## 6. Git 与数据安全

- Git 仓库已初始化，产品阶段性改动均保留在本地提交历史中。
- `.venv/`、`.env`、SQLite 数据库、原始材料、脱敏材料和 Streamlit 密钥均已纳入忽略规则。
- 提交前应再执行一次范围检查，确认没有个人信息或真实调研数据。

## 7. 当前限制

- 结论核验是确定性规则版，不应声称已接入大模型。
- 检索基于关键词和中文字符 n-gram，语义改写过大时可能召回不足。
- 只支持文字材料，未接入 ASR、OCR、多智能体或知识图谱。
- 已支持创建和切换本地多项目，但仍是单用户应用，没有账号和多人权限系统。

## 8. 下一阶段建议

1. 为用户项目提供可选的自定义检索评测集导入与运行方式。
2. 文字工作流稳定后，再评估可选大模型辅助改写或 OCR/ASR。
